export interface DrawParam {
  lineWeight: number
  lineColor: string
  bgColor: string,
  bgImage: string
}

export const initialDrawParam = {
  lineWeight: 1,
  lineColor: '#000',
  bgColor: '#fff',
  bgImage: null
}